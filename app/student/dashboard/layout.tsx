import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'My Studios | Sssion',
  description: 'Your Sssion studios — train on-demand, join live classes, and stay connected with your creators.',
}

export default function StudentDashboardLayout({ children }: { children: React.ReactNode }) {
  return children
}
