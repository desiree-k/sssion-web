import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Watch | Sssion',
  description: 'Watch studio classes on Sssion.',
}

export default function WatchLayout({ children }: { children: React.ReactNode }) {
  return children
}
