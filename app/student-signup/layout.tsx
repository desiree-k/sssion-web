import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Student Sign Up | Sssion',
  description: 'Create a free Sssion student account to join studios, watch classes, and train with movement creators.',
}

export default function StudentSignUpLayout({ children }: { children: React.ReactNode }) {
  return children
}
